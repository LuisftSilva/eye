{
  "name": "eye-portugal-live",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "test:syntax": "node scripts/check-syntax.cjs",
    "test:audit": "node scripts/audit-site.cjs",
    "check": "npm run test:syntax && npm run test:audit && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
